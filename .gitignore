/target
/dist
/node_modules
frontend/node_modules
frontend/dist
*.log
.DS_Store
# local cargo mirror config (the container build sets it via env instead)
/.cargo

# staged image build payloads (large static binaries copied from host tools)
/deploy/image/context/build/

# pkglab runtime data (registry storage / uploads)
*.sqlite
*.sqlite-shm
*.sqlite-wal
/tmp
