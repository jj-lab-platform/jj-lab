apiVersion: v1
kind: Service
metadata:
  name: {{ include "jj-lab.fullname" . }}
  labels:
    {{- include "jj-lab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "jj-lab.selectorLabels" . | nindent 4 }}